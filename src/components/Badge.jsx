import badge from "../images/Vector.svg";

const Badge = () => {
    return (
        <div className="mt-1">
            <img src={badge} alt="badge" />
        </div>
    );
};

export default Badge;
